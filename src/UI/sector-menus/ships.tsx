import { useState } from 'react'
import styles from '../../styles/user-interface-master.module.scss'
export const ShipsSectorMenu = () => {
    // const [hidden, setHidden] = (1)
    return (
        <div className=''>
           A POP UP
        </div>
    )

}